pipeline {
  agent any
  stages {
    stage('Restore Packages') {
      steps {
        echo 'Restore packages here...'
      }
    }
    stage('Build') {
      steps {
        echo 'Perform build here...'
      }
    }
  }
}
