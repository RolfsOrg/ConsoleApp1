pipeline {
  agent any
  stages {
    stage('Restore Packages') {
      steps {
        echo 'Restore packages here 2...'
      }
    }
    stage('Build') {
      steps {
        echo 'Perform build here...'
      }
    }
  }
}
